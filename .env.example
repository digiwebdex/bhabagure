# Local docker-compose settings. Copy to .env (git-ignored) and fill in the passwords.
# Placeholders only — this file is committed to a PUBLIC repository.

DB_DATABASE=bhabaghure
DB_USERNAME=bhabaghure_user
DB_PASSWORD=
DB_ROOT_PASSWORD=

FORWARD_DB_PORT=3307
FORWARD_REDIS_PORT=6379
