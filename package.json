{
  "name": "bhabaghure",
  "private": true,
  "description": "Bhabaghure Holidays ERP — admin, public website and customer portal",
  "workspaces": [
    "packages/*",
    "admin",
    "web"
  ],
  "scripts": {
    "dev:admin": "npm run dev --workspace admin",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace admin && npm run build --workspace web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "test:api": "cd api && php artisan test"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
